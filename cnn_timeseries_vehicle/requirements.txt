# Standalone TCN trainer - runtime dependencies.
# Install: pip install -r requirements.txt
torch>=2.1,<3
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6      # only for `--config run.yaml`; .json configs need nothing extra
